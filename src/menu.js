import React from 'react';
import './menu.css';

const Menu = () => {
    return(
        <div className="menu">
        <button>Projects</button>
        <button>About Me</button>
        <button>Contact</button>
        </div>
    )
}

export default Menu;